-- To run LUA scripts use:
-- const sha = cache.script_sha("example.lua")
-- const reply = await cache.evalsha(sha, 0);
-- Scripts loading should be enabled with config.cache.load_scripts

return 'hello_world_!'